spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/devops_toolbox
    username: devops
    password: devops

app:
  environment: prod

logging:
  level:
    root: WARN
    com.forge.devopstoolbox: INFO
